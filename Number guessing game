	• The program randomly selects a number between 1 and 10.
	• The user has to guess it.
    Give hints like "Too high" or "Too low".

	import random
	rand_num = random.randint(1,10)
	print('Welcomec to the number guessing game!!!!')
	print('I am thinking of a number between 1 to 10')
	user_input =input('Enter the number that you want to guess')
	print('The random number generated is:' , rand_num)
	
	if not(user_input.isdigit()) :
	    print('its not an integer')
	    
	user_input = int(user_input)
	
	if(user_input > rand_num) :
	    print('Your number is too far!!!!!')
	if(user_input < rand_num) :
	    print('Your were so close!!!!')
	if(user_input == rand_num) :
    print('Congratulations!!! Yo win!!!!')

